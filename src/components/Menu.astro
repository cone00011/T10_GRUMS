<section>
    <div class="logo">
            <img src="/GRUMS/logo/logo.png" alt="logo">
        </div>
       <nav>
        <ul class="menu">
            <li class="left"><a href="Menu.astro">menu</a></li>
            <li class="right"><a href="Events.astro">events</a></li>
        </ul>
        <div class="burger">
            <div class="line"></div>
            <div class="line"></div>
            <div class="line"></div>
        </div>
    </nav>
</section>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Logo */
.logo img {
  height: 50px;
  position: absolute;
  top: 15px;
  left: 30px;
  z-index: 10;
}

/* Menu styling */
nav {
  position: absolute;
  top: 30px;
  right: 20px;
  display: flex;
  align-items: center;
  z-index: 10;
}

.menu {
  display: flex;
  list-style: none;
}

.menu li {
  margin: 0 30px;
}

.menu a {
  text-decoration: none;
  color: #920000; /* Mørkerød som standard */
  font-family: 'Kepler Std', italic, sans-serif;
  font-size: 40px;
  font-weight: 400;
  
}

.menu a:hover {
  color: #F3F0E9; /* Cremefarve */
  text-shadow: -1px -1px 0 #920000, 1px -1px 0 #920000, -1px 1px 0 #920000, 1px 1px 0 #920000; /* Rød kant */
  transform: scale(1.1); /* Gør teksten større ved hover */
}

/* Burger Menu */
.burger {
  display: none; /* Skjules som standard */
  width: 30px;
  height: 20px;
  flex-direction: column;
  justify-content: space-between;
  cursor: pointer;
}

.burger .line {
  width: 100%;
  height: 2.5px;
  background-color: #920000;
}

.burger:hover .line {
  background-color: #a52a2a;
}

/* Responsiv styling */
@media (max-width: 1023px) {
  .menu {
    display: none; /* Skjuler desktop-menuen på mindre skærme */
  }

  .burger {
    display: flex; /* Viser burger-menuen */
  }
}

/* Mobilmenu */
.mobile-menu {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  background-color: rgba(128, 0, 0, 0.9); /* Semi-transparent rød baggrund */
  display: none;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.mobile-menu.open {
  display: flex; /* Viser mobilmenuen, når den er åben */
}

.mobile-menu a {
  color: white;
  font-size: 24px;
  margin: 20px 0;
  text-decoration: none;
}

.mobile-menu a:hover {
  color: #F3F0E9;
}
</style>